{
  "name": "Gmail",
  "description": "Search and read Gmail, download attachments to the current task, create drafts, and send messages. Account authorization is managed by Cindy.",
  "whenToUse": "Use for Gmail messages and attached logs/files. To analyze an attachment, read with download_attachments=true, then open the returned files. Connect an account in plugin details first.",
  "tools": {
    "gmail_accounts": {
      "description": "List the Gmail accounts connected to this plugin and their status. Accounts must be authorized separately in the Gmail plugin details."
    },
    "gmail": {
      "description": "search/read/send/draft/list_labels; read includes attachments. download_attachments saves selected attachment_ids (all by default) to the task or host save_dir. Max 16 MiB/file, 16 files/call; check complete/errors/remaining IDs. Sending requires user intent. modify_labels unavailable."
    }
  }
}
